<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="../public/_css/AdminCss.css" media="screen" type="text/css">
    <style>
        body{
            background-image: url('/img/musculoFormSelecao.jpg');
            background-position: center;
            background-size: cover;
        }
        .musculosForm{
            color: white;
            background-position:20% 35%;
            width: 100%;
            padding: 10px;
        }
        .centralizar{
            display: flex;
            width: 100%;
            justify-content: center;
        }

    </style>
</head>
<div class="centralizar">

<h1 style="color: #f7f7f7;">Escolha o seu treino</h1>
</div>
<div class="musculosForm card-body" >
    <form action="/treinos/geraTreino" method="post" >
        {{#each user}}
            <input type="hidden" name="{{@key}}" value="{{this}}">
        {{/each}}
    
        <select class="form-control" name="musculo" id="musculo">
            {{#each musculos}}
               <option value="{{nome}}">{{nome}}</option>
            {{else}}

            {{/each}}
        </select> <br>
        <select class="form-control" name="musculo1" id="musculo1">
            {{#each musculos}}
               <option value="{{nome}}">{{nome}}</option>
            {{else}}

            {{/each}}
        </select> <br>
        <select class="form-control" name="musculo2" id="musculo2">
            {{#each musculos}}
               <option value="{{nome}}">{{nome}}</option>
            {{else}}

            {{/each}}
        </select> <br>
        <button type="submit" class="btn btn-secondary">Escolher musculos</button>
       
    </form>   
     <form action="/users/paginaInicial" method="post" class="mt-4">
        {{#each user}}
            <input type="hidden" name="{{@key}}" value="{{this}}">
        {{/each}}
        <button type="submit" class="btn btn-secondary">Voltar para a pagina inicial</button>
     </form>
</div>
    
 
 