<h1>Seleção de musculos</h1>
<form action="/treinos/geraTreino" method="post" class="form-control">
    
        <select class="form-control" name="musculo" id="musculo">
            {{#each musculos}}
               <option value="{{nome}}">{{nome}}</option>
            {{else}}

            {{/each}}
        </select> <br>
        <select class="form-control" name="musculo1" id="musculo1">
            {{#each musculos}}
               <option value="{{nome}}">{{nome}}</option>
            {{else}}

            {{/each}}
        </select> <br>
        <select class="form-control" name="musculo2" id="musculo2">
            {{#each musculos}}
               <option value="{{nome}}">{{nome}}</option>
            {{else}}

            {{/each}}
        </select> <br>
    <button type="submit" class="btn btn-secondary">Gerar treino</button>
</form>       
 
 